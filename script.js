const links = document.querySelectorAll('.icones a')
links.forEach(element => {
    element.classList.add('ambos')
})


const addi = document.querySelector('main')
addi.classList.add('container')

const desc = document.querySelector('main div')
desc.classList.add('descrition')